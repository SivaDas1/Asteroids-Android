import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.starrail.asteroids3d',
  appName: 'Asteroids 3D Star Rail',
  webDir: 'dist'
};

export default config;
